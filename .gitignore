# Secrets (real values, never commit)
secrets*
# Backup archives (contain secrets + database dumps)
backups/
.env
# Local data volumes
pg_data/
pgadmin_data/
redis_data/
# Node
node_modules/
# Build output
dist/
# Go compiled binary
budgeteer-backend
# Opencode AI assistant config
.opencode/
# Dev certificates (self-signed, generated locally)
certs/
# TypeScript incremental build cache
*.tsbuildinfo
